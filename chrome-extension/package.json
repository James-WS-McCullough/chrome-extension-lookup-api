{
  "name": "chrome-extension-author-lookup",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/ui/popup.ts --bundle --outfile=dist/popup.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "jsdom": "^24.1.3",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
