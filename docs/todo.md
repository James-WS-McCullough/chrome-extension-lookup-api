# Todo

## Hebert Suggestions
### Chrome Extension
 - [x] ⁠Since you are using atomic design, I think you should apply the template/page, instead of having everything in the app.vue
 - [x] I would place your state management in a shared store file, with a proper enum instead of string literals, since everything is so fancy :3
 - [] SCSS is fine as really nice in each bits, but I would try to use Tailwind maybe? Just a suggestion, because it is very welcome in the market if you know
 - [x] I would have a env file instead of using process.env directly in the files (like in the api)
 - [x] Since you brought DDD in the game, you could have a HTTP Fetch and a Gateway for the API, I will send you an example
 - [x] Some colours could have a shared SCSS, like a theme. Just to show-off lol