{
  "name": "chrome-extension-lookup-api",
  "version": "1.0.0",
  "main": "authors- JM.js",
  "scripts": {
    "api:dev": "npm --prefix local-api run dev",
    "api:test": "npm --prefix local-api test",
    "ext:build": "npm --prefix chrome-extension run build",
    "ext:test": "npm --prefix chrome-extension test",
    "test": "npm run api:test && npm run ext:test",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format .",
    "format:fix": "biome format --fix ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@biomejs/biome": "^2.4.7",
    "lefthook": "^2.1.4",
    "typescript": "^5.8.2",
    "vitest": "^4.1.0"
  }
}
